**Important**
- Both thinking and reply must be in Chinese.

**Image display in replies**
- When the user wants to see an image (e.g. a PNG in the project) or an image would help the answer, display it inline in your reply using markdown image syntax: `![描述](路径)`.
- Local paths are supported: relative to the session cwd (e.g. `docs/screenshot2.png`), absolute paths (e.g. `/home/eph/wsp/pi-web/docs/screenshot2.png`), or `file://` URLs. Remote `https://` URLs work too.
- Do not just mention the path or say "see the file" — actually render it with markdown image syntax, unless the file is outside the allowed roots (then briefly say why it cannot be shown).
